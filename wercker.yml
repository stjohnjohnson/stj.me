box: ruby

build:
  steps:
    # Install gem dependencies
    - bundle-install

    # Generate site, no drafts in here
    - script:
        name: generate sites
        code: bundle exec jekyll build --trace
